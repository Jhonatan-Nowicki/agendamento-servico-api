services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: agendamento_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - agendamento_postgres_data:/var/lib/postgresql/data
volumes:
  agendamento_postgres_data: